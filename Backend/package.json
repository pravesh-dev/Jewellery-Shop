{
  // Project name
  "name": "backend",
  // Project version
  "version": "1.0.0",
  // Project description
  "description": "",
  // Entry point of the application
  "main": "server.js",
  // Specify the module system to use
  "type": "module",
  // Scripts to run the application
  "scripts": {
    // Script to start the application
    "start": "node server.js",
    // Script to start the application with nodemon for development
    "server": "nodemon server.js"
  },
  // Author information
  "author": "",
  // License under which the project is released
  "license": "ISC",
  // Dependencies required by the project
  "dependencies": {
    // Dependency for password hashing
    "bcrypt": "^5.1.1",
    // Dependency for cloud storage
    "cloudinary": "^2.6.0",
    // Dependency for enabling CORS
    "cors": "^2.8.5",
    // Dependency for environment variables
    "dotenv": "^16.4.7",
    // Dependency for the Express framework
    "express": "^4.21.2",
    // Dependency for JSON Web Tokens
    "jsonwebtoken": "^9.0.2",
    // Dependency for MongoDB interactions
    "mongoose": "^8.12.1",
    // Dependency for multipart/form-data handling
    "multer": "^1.4.5-lts.1",
    // Dependency for automatic server restarts
    "nodemon": "^3.1.9",
    // Dependency for Razorpay payment gateway
    "razorpay": "^2.9.6",
    // Dependency for Stripe payment gateway
    "stripe": "^17.7.0",
    // Dependency for data validation
    "validator": "^13.12.0"
  }
}
