import React from 'react' // Importing React from the react library

// Defining the App component
function App() {
  // Returning the JSX for the App component
  return (
    <div className='bg-red-500'> // Div with a red background
      home admin page // Text content of the div
    </div>
  )
}

// Exporting the App component as the default export
export default App
