import React from 'react' // Importing React from the react library

// Defining the List component
const List = () => {
  // Returning the JSX for the List component
  return (
    <div>
      List page // Text content of the div
    </div>
  )
}

// Exporting the List component as the default export
export default List
